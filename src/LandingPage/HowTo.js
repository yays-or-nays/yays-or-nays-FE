import React from 'react';
import {
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
} from 'react-native';

const HowTo = () => {
  return (
    <View style={styles.view}>
      <Text></Text>
    </View>
  )
}

const styles = StyleSheet.create({
  view: {
    height: '85%'
  }
})

export default HowTo;
